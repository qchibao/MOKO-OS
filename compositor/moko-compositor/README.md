# moko-compositor

Future native MOKO Wayland compositor. Not implemented in the first Developer Preview.

The bootstrap ISO uses Cage only to host the full-screen `moko-shell`. Do not add GNOME/KDE as a replacement compositor/session.
