#!/bin/sh
set -eu
cat > /etc/greetd/config.toml <<'CFG'
[terminal]
vt = 1

[default_session]
command = "cage -s -- /usr/local/bin/moko-session"
user = "moko"
CFG
systemctl enable greetd.service || true
systemctl set-default graphical.target || true
