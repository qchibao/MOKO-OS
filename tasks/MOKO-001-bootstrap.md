# MOKO-001 — Repository bootstrap
State: DONE

Create the baseline repository, architecture rules, design rules, build scripts and task system.

## Validation
- Repository structure exists.
- `AGENTS.md`, architecture and roadmap exist.
- No GNOME/KDE theme structure is used as the product core.
