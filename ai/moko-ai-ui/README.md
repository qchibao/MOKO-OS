# MOKO AI UI
The first visual surface is embedded in `moko-shell`. The reusable
`AiController` in `ai/ui` reports daemon connectivity, processing, responses
and action failures to QML without putting D-Bus policy in the view.
